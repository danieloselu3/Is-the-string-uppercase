language : python
python :
  - "3.5"
cache : pip
install :
  - pip install -r requirements.txt
  - pip install coveralls
script :
  - python test_is_the_string_uppercase.py -v
after_success:
  - coveralls
